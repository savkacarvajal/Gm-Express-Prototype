---
import Layout from '../layouts/Layout.astro';
import ServiceCard from '../components/ServiceCard.astro';
import { services } from '../data/services';
---

<Layout title="GMExpress - Servicios">
	<section class="page-header">
		<div class="container">
			<h1>⚙️ Nuestros Servicios</h1>
			<p>Soluciones tecnológicas profesionales para tu negocio</p>
		</div>
	</section>

	<section class="services-section">
		<div class="container">
			<div class="intro">
				<h2>Servicios Profesionales de Calidad</h2>
				<p>
					Ofrecemos una amplia gama de servicios tecnológicos diseñados para optimizar 
					tu infraestructura digital y potenciar tu negocio. Nuestro equipo de expertos 
					está listo para ayudarte a alcanzar tus objetivos.
				</p>
			</div>

			<div class="services-grid">
				{services.map(service => (
					<ServiceCard service={service} />
				))}
			</div>

			<div class="cta-box">
				<h3>¿Necesitas un servicio personalizado?</h3>
				<p>Contáctanos y crearemos una solución a tu medida</p>
				<button class="contact-btn">Contactar Ahora</button>
			</div>
		</div>
	</section>
</Layout>

<style>
	.page-header {
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		color: white;
		padding: 3rem 0;
		text-align: center;
	}

	.page-header h1 {
		font-size: 3rem;
		margin-bottom: 0.5rem;
	}

	.page-header p {
		font-size: 1.2rem;
		opacity: 0.9;
	}

	.services-section {
		padding: 3rem 0;
	}

	.intro {
		text-align: center;
		max-width: 800px;
		margin: 0 auto 3rem;
		padding: 2rem;
		background: white;
		border-radius: 12px;
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
	}

	.intro h2 {
		color: #1e3c72;
		font-size: 2rem;
		margin-bottom: 1rem;
	}

	.intro p {
		color: #666;
		font-size: 1.1rem;
		line-height: 1.8;
	}

	.services-grid {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
		gap: 2rem;
		margin-bottom: 3rem;
	}

	.cta-box {
		background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
		color: white;
		padding: 3rem;
		border-radius: 12px;
		text-align: center;
		box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
	}

	.cta-box h3 {
		font-size: 2rem;
		margin-bottom: 1rem;
	}

	.cta-box p {
		font-size: 1.1rem;
		margin-bottom: 2rem;
		opacity: 0.9;
	}

	.contact-btn {
		padding: 1rem 2.5rem;
		background: white;
		color: #1e3c72;
		border: none;
		border-radius: 10px;
		font-size: 1.1rem;
		font-weight: 600;
		cursor: pointer;
		transition: all 0.3s;
		box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
	}

	.contact-btn:hover {
		transform: translateY(-3px);
		box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
	}

	@media (max-width: 768px) {
		.page-header h1 {
			font-size: 2rem;
		}

		.services-grid {
			grid-template-columns: 1fr;
		}

		.intro h2 {
			font-size: 1.5rem;
		}

		.cta-box h3 {
			font-size: 1.5rem;
		}
	}
</style>

<script>
	const contactBtn = document.querySelector('.contact-btn');
	if (contactBtn) {
		contactBtn.addEventListener('click', () => {
			alert('¡Gracias por tu interés! En un proyecto real, aquí se abriría un formulario de contacto o se redigiría a WhatsApp/Email.');
		});
	}
</script>
